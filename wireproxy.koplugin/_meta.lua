local _ = require("gettext")
return {
    name = "wireproxy",
    fullname = _("WireProxy VPN"),
    description = _([[Enables WireGuard-based tunneling via wireproxy on Android devices. Handles binary installation, lifecycle management, and proxy configuration.]]),
}
